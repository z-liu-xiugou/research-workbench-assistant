# Changelog

## 0.1.0-alpha.7 — 2026-09-08

- 补齐 v0.1 六项 Issue 的验收缺口；新增配置 v1、事件/记录 v2 Schema 与虚构示例，补充 fact/artifact 类型。
- 进行中/已确认要求可定位证据；已确认要求用户明确确认出处。文件检查存在及边界，事件检查历史 ID，URL/DOI 只做格式校验，不证明事实或用户身份。
- 旧 v1 事件只读兼容，audit 报复核警告，不自动迁移；新写入及修订使用 v2。证据文件遗失不会使历史无法读取，audit 会提示。
- 新增 DECISIONS_AND_ISSUES 派生视图，全部视图删除重建一致性回归。
- resume 输出 JSON 读取计划；缺失/空/过长断点和明确触发原因路由至 review_required，正常续接只读一个有界断点，不扫描事件库。
- 新增已知密钥与个人路径扫描、默认 Git 忽略回归，纳入 Windows/Linux CI。扫描并非完整隐私保证。

## 0.1.0-alpha.6 — 2026-09-08

- 内置 discover：通过 Crossref 按主题与可选年份范围联网检索，单次获取前 1–50 条。
- 保存来源、请求参数、时间、元数据、总命中、重复和跳过原因；与批内、历史候选及当前笔记按 DOI 去重。
- 新增 CANDIDATES.md/JSON、candidates 和 candidate-review；筛选保留/排除/重新待筛选，有理由和历史。
- 候选与 paper 笔记分离；实际生成同 DOI 笔记后显示 noted，不代表已读或全文阅读。
- 批次单事件保存、网络超时/限流/格式异常显式失败、响应大小限制、默认不跟随跳转；不自动重试。
- 只有 discover 联网发送查询参数；候选随原有备份恢复。其他来源、全文下载和 PDF 解析仍未内置。
- 新增 16 项离线测试；公开主题真实联网验证新增 3 篇，重复执行新增 0 篇，视图检查通过。

## 0.1.0-alpha.5 — 2026-09-08

- 所有记录支持 links 显式关联，包含目标 ID、关系类型和理由；拒绝悬空目标与重复关系。
- 新增 related 正向/反向查询、graph JSON 输出，以及 RELATIONS.md/JSON 派生视图。
- 引用准确历史版本，不静默重定向；提示 is_current/latest_id 供人工核对。
- 关联随事件修订、备份和恢复；修订来源时省略 links 表示不保留其当前关联，历史仍可查。
- 新增 7 项关联回归测试；这是显式关系管理，不是自动推断或交互式图谱。

## 0.1.0-alpha.4 — 2026-09-08

- 新增 backup/restore，备份受管理的配置、事件、手写笔记和项目约定，恢复到新目录。
- 校验文件路径白名单、SHA-256 和事件结构；拒绝覆盖备份或已有恢复目标。
- 恢复重建派生视图，并保留默认 Git 忽略规则；不打包论文、实验数据或其他自建文件。
- 原始 events 目录缺失时明确报错，不再误判成空工作台。
- 备份未加密，不是防篡改签名；最多 64 MiB/10000 文件。写出或恢复复制失败可能留下不完整新文件/目录，详情见使用指南。
- 新增 10 项备份恢复回归测试。

## 0.1.0-alpha.3 — 2026-09-08

- task_state 区分待办、执行中、完成和取消，与研究判断 status 分离；完成要求提供证据。
- 新增 tasks 命令和 TASKS.md 分组清单；旧任务显示未指定，不擅自判断完成。
- 续接断点改为限长摘要和原始事件链接，不嵌入文献全文；附未关闭任务数量和最多 5 项导航。
- 新增 6 项任务、修订、旧记录兼容、CLI 和断点长度回归测试。
- 新版可读取旧记录；升级先备份再 render。研究结论真实性仍由用户核验。

## 0.1.0-alpha.2

- 文献新增可选作者、年份、DOI、期刊/会议和标签字段；兼容原有笔记输入。
- DOI 格式归一化及当前有效记录去重，支持通过 supersedes 修订同一论文。
- 自动生成 PAPER_INDEX.md 文献目录，隐藏被替代版本但保留历史笔记。
- 新增元数据校验、重复入库拒绝、修订目录和输入对象不被修改的测试。
- DOI 仅做本地格式检查，未提供联网真实性核验；无 DOI 的记录不自动去重。

## 0.1.0-alpha.1 — 2026-09-07

首个本地 Codex 可用版本，适合个人试用。

### 已实现

- 自包含 research-workbench Skill 和拒绝覆盖的安装器。
- Python 3.10+ 标准库工具：init、record、resume、search、audit、render。
- JSON 原始事件、替代修订链、自动生成的工作台与分类台账。
- 由同一来源生成 Markdown/JSON 文献笔记，保留阅读范围。
- 写入锁、单文件原子替换、失败后视图重建。
- 虚构示例、双语首页、安装说明、社区推广文案和自动测试。

### 已知限制

- 不自带联网检索、PDF 解析、DOI 去重、Zotero、知识图谱和后台调度。
- 多个视图不构成整体事务；异常退出后可 audit/render 恢复。
- 证据真实性和用户已读状态不由程序自动验证。
- 早期 schema，不承诺后续版本无需迁移。
