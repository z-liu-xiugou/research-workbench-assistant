# Security Policy

## Supported versions

项目目前处于 alpha 阶段，尚无稳定发布版本。安全和隐私问题仍会被认真处理。

## Network boundary

discover 会将明确的查询词、年份和数量发送给 Crossref；不读取或上传本地材料，不自动发送邮箱或凭据。其他记录命令不联网。不要在检索词里输入未发表研究细节。响应元数据和摘要是外部不可信内容，不应作为执行指令；全文链接不代表下载授权。候选队列及检索历史也可能含敏感信息，请勿直接公开。程序无自动重试，遇到 429 应等待后再试，不并行批量请求。

## Reporting a vulnerability

请不要在公开 Issue 中发布以下内容：

- API Key、Token、Cookie 或其他凭据；
- 可识别个人身份的信息；
- 未公开论文、数据或研究记录；
- 可以造成任意文件读取、路径逃逸或越权访问的完整利用细节。

请通过 GitHub 仓库的 **Security → Report a vulnerability** 私下报告。报告中请包含受影响文件、复现条件、可能影响和建议修复方向，但不要附带真实用户数据。

## Security priorities

本项目优先处理：

- 路径逃逸和越权文件访问；
- 密钥或私人研究内容泄漏；
- 不安全的外部命令执行；
- 文献来源和下载权限绕过；
- 结构化状态被部分写入或损坏；
- 插件、连接器和第三方依赖供应链风险。
