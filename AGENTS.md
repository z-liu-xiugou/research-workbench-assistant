# AGENTS.md

## 项目目标

本仓库用于开发一个面向研究生的通用科研助手。核心不是替用户决定研究内容，而是把 AI 参与的科研过程保存为可追溯、可恢复、可核验的本地记录，并提供文献发现、结构化笔记和研究记忆网络。

## 当前阶段

- 当前属于开源项目规划与迁移阶段，不得把规划中的功能描述为已经实现。
- 先实现科研记忆和工作台最小闭环，再扩展文献发现、文献笔记和知识网络。
- `_private_reference/` 保存用户现有助手与工作台的只读副本，只能用于迁移审计，不得提交、发布或直接改造成公开示例。

## 固定边界

- 不修改本仓库之外的原始科研助手、原课题工作台或原始文献库。
- 不把个人绝对路径、账号、密钥、Zotero 标识、未公开研究材料、论文全文或真实项目记录写入公开文件。
- 所有项目状态必须区分：`已确认`、`进行中`、`计划中`、`候选方案`、`待确认`、`AI建议`。
- AI 不得自行把候选方案升级为已确认，不得把生成笔记等同于用户已经阅读。
- 工作日志只追加；当前状态可以重建；跨对话断点是导航入口，不是事实总库。
- 写入多个记录文件时，优先由结构化事实生成 Markdown 视图，避免分别改写造成冲突。

## 开发与说明方式

- 默认使用清晰、具体的中文解释，面向编程经验有限的研究生。
- 修改代码时说明原问题、修改位置、关键代码、解决原理、验证结果和影响范围。
- Windows 命令使用 PowerShell；中文 Python 工具显式使用 UTF-8。
- 新功能必须有可观察的验收条件；涉及文件写入时要验证 Schema、路径边界和失败后的完整性。
